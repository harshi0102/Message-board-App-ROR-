<h2><%= @message.title %></h2>
<p><%= @message.description %></p>
<%= link_to "Back", root_path %>
<%= link_to "Edit", edit_message_path(@message) %>
<%= button_to "Delete Message", message_path(@message), method: :delete, onclick: "return confirm('Are you sure?');" %>
